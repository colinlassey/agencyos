# Next Steps
1. Implement client creation modal with form validation and optimistic updates.
2. Wire task board drag-and-drop with persisted state transitions.
3. Add Socket.IO event emission in API handlers and subscribe on React Query cache updates.
4. Build Google Calendar OAuth flow and persist integration tokens.
5. Implement file upload UI with S3 signed URL mutation and progress indicator.
6. Build notification bell component with unread count and mark-as-read interaction.
7. Add server-side filtering/pagination for clients and projects lists.
8. Expand workload heatmap to compute capacity from user profiles.
9. Integrate automated accessibility tests (axe) for core screens.
10. Configure CI pipeline to run Prisma migrations, seeds, and Vitest suites.
